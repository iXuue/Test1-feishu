# 2026-08-15

## CodeCub Phase 1 — Long-Horizon Runtime 实施完成

依据 `CODECUB_2_0_LONG_HORIZON_RUNTIME_ROADMAP.md` + `CODECUB_PHASE1_LONG_HORIZON_RUNTIME_IMPLEMENTATION_PROMPT.md` 完成 Phase 1。

### 新增
- `codecub/watchdog.py`：独立 Progress Watchdog（唯一 stuck 决策来源）。
  - 状态机 NORMAL → STUCK_SUSPECTED → RECOVERY → (progress→NORMAL | 无进展→STUCK_CONFIRMED)。
  - 检测 identical / alternating / verification / action-error / semantic-read / search loop + semantic no-progress window。
  - 集中配置常量（window=6, identical=5, alternating=6, verification=4, action-error=4, recovery=6, overlap=0.8）。
- `tests/test_watchdog.py`：25 项 Phase 1 专项测试（runtime mode / 长链路 55 步 / loop 检测 / recovery / safety / regression）。

### 修改
- `codecub/runtime.py`：
  - `max_steps` 默认 None（interactive 无固定预算）；新增 `runtime_mode`（interactive/experiment）、`emergency_cap`（默认 500）。
  - 主循环改为 while True + 顶部停机判断：experiment/显式 max_steps 用固定预算；interactive 用 emergency cap + attempts 兜底。
  - 删除旧 `repeated_no_progress` 直接停机与 `SEMANTIC_REPEAT_HARD_STOP_THRESHOLD` hard stop；warning 保留为纯 observability。
  - 新增 `_advance_watchdog` / `stop_stuck_confirmed_run` / `stop_emergency_cap_run`；RECOVERY_TURN_PROMPT 不含任务答案。
  - report 新增 runtime_mode / effective_step_budget / emergency_cap / watchdog snapshot；trace 新增 progress_detected / stuck_suspected / recovery_turn_started / recovery_turn_finished / stuck_confirmed / emergency_cap_reached。
- `codecub/cli.py`：`--max-steps` 默认 None（interactive 不再继承小固定预算）。
- `codecub/experiments/runner.py`：make_agent 显式 `runtime_mode=RUNTIME_MODE_EXPERIMENT`（24-step 语义不变）。
- `codecub/experiments/metrics.py`：新增 stuck_suspected/recovery/confirmed 指标提取与 summarize。
- `tests/test_pico.py`：更新 2 个旧测试适配 Phase 1 语义（默认无预算、identical loop 改为 suspected→recovery）。

### 验证结果
- targeted test_watchdog 25/25 ✓；test_pico 107+1skipped ✓；test_experiments 14/14 ✓；full pytest 260 passed / 2 failed（环境相关，非本改动）；Harness Regression 12/12 ✓；ruff check . 全绿；secret scan clean。
- 2 个 full pytest 失败为沙箱环境问题：`test_evaluator`（shutil.rmtree 被沙箱 safe-delete 拦截）、`test_safety_invariants::test_symlink_path_traversal_is_rejected`（Windows symlink）。改动前后均失败，与 Phase 1 无关。

### ⚠️ Git 事故（重要）
- 执行 `git stash push` 验证预存失败时，stash 报 fatal 且沙箱环境将 `.git/objects` 大部分对象与 `refs/heads` 清空（本地未推送的 2 个 commit 历史对象丢失：34f4f8c "Add fresh formal experiment holdout"、7621201 "Shorten experiment workspace paths on Windows"）。
- 已通过 `git fetch origin` + 手动重建 ref（HEAD=d5600f0，main=e9620dc）+ `git read-tree HEAD` 恢复：git log/status 正常，**工作区文件与全部未提交改动（含丢失 commit 的内容）完好**。
- 待办：需重新 commit 工作区中 3 个文件的改动（experiments/tasks.py、experiments/runner.py、tests/test_experiments.py 共 54 insertions 对应丢失 commit 的内容）。
- 教训：在此 Windows 沙箱环境下避免对 .git 做 stash/gc 等写操作；改文件前先备份（本轮已备份到 E:\codex_backup\20260815-224827-phase1-long-horizon-runtime）。
